# Maze Puzzle

## Run

```powershell
py .\main.py
```

The script:
- reads input from `puzzle_input.txt`;
- prints `Part 1` (maximum distance along the main loop);
- prints `Part 2` (number of tiles enclosed by the loop).

It also includes short self-tests based on the AoC examples. They run automatically before processing the main input.

